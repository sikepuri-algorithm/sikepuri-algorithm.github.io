services:
  app:
    build: .
    volumes:
      - .:/app
      - node_modules_app:/app/node_modules
    ports:
      - 3000:3000
    command: npm start
    tty: true

  black:
    build:
      context: .
      dockerfile: Dockerfile-black
    volumes:
      - .:/app
    command: black --check .

volumes:
  node_modules_app:
