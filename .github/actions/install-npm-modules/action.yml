name: Install Node Modules

runs:
  using: composite
  steps:
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: latest
        cache: npm

    - name: Install Node Modules
      run: npm ci
      shell: bash
